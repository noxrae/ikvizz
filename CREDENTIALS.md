# 🜁 Ikvizz — Demo Credentials

App URL: **http://localhost:4321**

All demo accounts use the same password: **`aether123`**

| Username | Password    | Who they are |
|----------|-------------|--------------|
| `aarav`  | `aether123` | ⭐ **Main demo account — start here.** Has promises due, waiting conversations, a startup space. |
| `rahul`  | `aether123` | Close friend & co-founder (inner circle, in 🎯 Deep Work) |
| `maya`   | `aether123` | Colleague (professional persona, at the 🏋️ gym — inner circle only) |
| `amma`   | `aether123` | Family (inner circle, 😴 sleeping) |
| `vikram` | `aether123` | Investor (outer orbit, 📅 in meetings) |

## Best two-window demo

1. Window 1 → sign in as `aarav`
2. Window 2 (incognito/another browser) → sign in as `rahul`
3. Open each other's chat and type — watch **"is thinking… / writing…"** live
4. Send `URGENT: call moved to today!` from rahul → aarav gets a 🔴 critical alert
5. Send `nice weather today` → no interruption. That's Priority Streams.

> ⚠️ These are demo accounts for local development only — they sign in
> **locally** (username, not email) and keep working with Supabase enabled.
> Real accounts: click **"Create an account"** — with Supabase configured this
> registers through Supabase Auth (email + confirmation link), then signing in
> with that email enters Ikvizz.
> To reset the whole demo world: stop the server, delete `data/aether.db*`, run `npm run seed`.
